// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/3/a/RAM64.hdl
/**
 * Memory of sixty four 16-bit registers.
 * If load is asserted, the value of the register selected by
 * address is set to in; Otherwise, the value does not change.
 * The value of the selected register is emitted by out.
 */
CHIP myRAM64 {
    IN in[16], load, address[6];
    OUT out[16];

    PARTS:

    myDMux8Way(in=load, sel=address[0..2], a=out1, b=out2,c=out3,d=out4,e=out5,f=out6,g=out7,h=out8);

    myRAM8(in=in, load=out1, address=address[3..5], out = outa);
    myRAM8(in=in, load=out2, address=address[3..5], out = outb);
    myRAM8(in=in, load=out3,address=address[3..5], out = outc);
    myRAM8(in=in, load=out4,address=address[3..5], out = outd);
    myRAM8(in=in, load=out5,address=address[3..5], out = oute);
    myRAM8(in=in, load=out6,address=address[3..5], out = outf);
    myRAM8(in=in, load=out7,address=address[3..5], out = outg);
    myRAM8(in=in, load=out8,address=address[3..5], out = outh);

    myMux8Way16(a=outa, b=outb, c=outc, d=outd, e=oute, f=outf, g=outg, h=outh, sel=address[0..2], out=out);
}
